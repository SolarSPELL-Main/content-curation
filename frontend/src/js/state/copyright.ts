import { createSlice, PayloadAction } from '@reduxjs/toolkit';

export const copyrightSlice = createSlice({
    name: "copyright",
    initialState: {},
    reducers: {}
})

export const {} = copyrightSlice.actions

export default copyrightSlice.reducer
